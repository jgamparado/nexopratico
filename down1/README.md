# Downsell — Kit Entre Sessões

Rota: `/down1/`. Mesmo kit completo por 12,90 €, menos 5 € do que a oferta anterior.

Reutiliza o design system, os componentes de `up1` e o mockup WebP. A recusa de `/up1/` conduz a esta página.

Aguardar instruções do utilizador para preencher `acceptUrl` e `declineUrl` em `config.js`. Os controlos permanecem inativos enquanto forem placeholders. Usar o checkout próprio da oferta de 12,90 €.

Build: `python3 scripts/build.py` na raiz do site; saída em `dist/down1/`.
